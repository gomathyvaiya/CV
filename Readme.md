# Canny Edge Detection Web App

A Flask-based web app to perform Canny edge detection on user-uploaded images, with optional thresholding.

## Installation

```bash
pip install -r requirements.txt
python app.py
